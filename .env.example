# OpenAI API key used for fact extraction. Required by `npm run extract`.
OPENAI_API_KEY=

# Model used for extraction. Must be an explicit, changeable value - never hardcode
# a model the user cannot override.
OPENAI_MODEL=gpt-4.1
